# Legendary Workshop V9

## المعتمد في هذه النسخة
- الموقع والبوت على PostgreSQL / Railway بدون Supabase.
- الصلاحيات الحية حسب Discord: Owner ثم Boss ثم HR ثم Employee، مع حالة الموظف من قاعدة البيانات.
- تصميم لوحات الموقع بعد تسجيل الدخول بقائمة جانبية، صورة Discord، الاسم واسم المستخدم والرتبة.
- صفحات Employee / HR / Boss-Owner بهوية Legendary الداكنة والسلفر.
- عرض صور وأسماء Discord في قوائم الموظفين والمتصدرين والموظفين المتصلين والمجازين.
- ساعات مباشرة أسبوعية ودائمة للموظف.
- لوق الخروج يعرض مدة الشفت وإجمالي ساعات الأسبوع، وكذلك الخروج الإجباري.
- `/set-online-panel` لاختيار روم لوحة المسجلين دخول الآن + الموظفين في إجازة.
- `/set-admin-log` لاختيار روم اللوق الإداري الشامل.
- اللوق الإداري يسجل حركات Discord والموقع والنظام في قاعدة البيانات ويرسلها إلى روم اللوق المحدد.
- الإجازة تطلب عدد الأيام فقط + سبب اختياري؛ تاريخ البداية والنهاية يحسبان من لحظة قبول HR.
- كسر الإجازة والعودة التلقائية بعد انتهاء المدة.
- بيع عدة: صورة فاتورة فقط، OCR لـ MONEY AMOUNT، تسجيل تلقائي بنقطة واحدة بدون تأكيد.
- تعديل مركبة: صورة الفاتورة ثم صورة المركبة، تسجيل تلقائي بخمس نقاط بدون تأكيد.
- صور العمليات التي يرسلها الموظف تحذف من روم اللوحة بعد المعالجة وتبقى نسخة الإثبات في اللوق.
- الرسالة المؤقتة للعملية يتم حذفها بعد نجاح التسجيل.
- التقديم، التوظيف، المقابلات، التكتات، الإنذارات، الفصل، الموارد، إغلاق الأسبوع وبقية الأنظمة السابقة باقية.

## بعد الرفع
1. Railway: اترك `Pre-deploy Command` = `npm run db:setup` في خدمة البوت والموقع.
2. ارفع الكود إلى GitHub وانتظر نجاح Deploy للخدمتين.
3. شغّل `/setup-logs` و `/setup-panels` عند الحاجة لتحديث اللوحات.
4. شغّل `/set-admin-log` واختر روم اللوق الإداري.
5. شغّل `/set-online-panel` واختر روم لوحة المسجلين دخول والإجازات.

## V10 - جلسة دخول الموقع
- تسجيل الدخول عبر Discord صار محفوظًا افتراضيًا لمدة 30 يومًا بدل طلب الدخول بعد تحديث الصفحة.
- الكوكي `legendary_session` تُرسل صراحةً مع استجابة OAuth لضمان حفظها على Railway.
- صفحة `/login` تعيد المستخدم المسجل مباشرة إلى `/portal` بدل عرض زر تسجيل الدخول من جديد.
- لا تغيّر `SESSION_SECRET` بين عمليات النشر؛ تغييره يبطل جميع الجلسات الحالية.
- يمكن تغيير مدة الجلسة بمتغير `SESSION_DAYS` (الافتراضي 30).

## V11 - ربط رومات التقديم
- أضيف الأمر `/set-application-channels` لاختيار روم `طلبات الموقع` وروم `مراجعة التقديم` يدويًا، بدون الاعتماد على اسم الروم أو مكانه.
- عند تشغيل الأمر تتم مزامنة أي طلب Pending محفوظ في قاعدة البيانات ولم تصل رسالته إلى Discord.
- API التقديم يعيد محاولة إرسال الطلب القديم إذا كان محفوظًا بدون `discord_message_id` بدل تركه عالقًا.
